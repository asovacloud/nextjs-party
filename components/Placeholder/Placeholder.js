const Placeholder = ({componentName}) => (
    <div className="placeholder-notice">
        <p className="text-center">The component <strong>{componentName}</strong> has not been created yet.</p>
    </div>
);

export default Placeholder;